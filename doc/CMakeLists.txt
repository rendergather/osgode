FILE(READ ${OSGODE_TEMP_DOXYGEN_INPUT} files)

STRING(REGEX REPLACE "\n" ";" files ${files})

FOREACH(file ${files})
    SET(OSGODE_DOXYGEN_INPUT_FILES "${OSGODE_DOXYGEN_INPUT_FILES} \\\n${file}")
ENDFOREACH(file ${files})


SET(OSGODE_DOXYGEN_INPUT_FILTER "${PROJECT_SOURCE_DIR}/doc/filter.py")


CONFIGURE_FILE("${PROJECT_SOURCE_DIR}/doc/Doxyfile.in" "${PROJECT_BINARY_DIR}/doc/Doxyfile")

FILE(REMOVE ${OSGODE_TEMP_DOXYGEN_INPUT})
